<meta name="htmx-config" content='{"selfRequestsOnly": false}'>
<link rel="stylesheet" href="{{API_URL}}/static/object.css">
<link rel="stylesheet" href="{{API_URL}}/static/htmx_loader.css">
<script src="{{API_URL}}/htmx/htmx.min.js"></script>

<!-- El contenedor principal se mantiene ESTABLE, no se reemplaza -->
<div id="sidebar-container" class="tw-preflight tw-w-full tw-text-black tw-bg-transparent dark:tw-bg-[#424242] dark:tw-text-white">
    
    <!-- Loader: Se activa con htmx-request -->
    <div id="sidebar-loader" class="htmx-indicator tw-w-full tw-flex tw-flex-col tw-items-center tw-justify-center tw-py-6 tw-gap-2">
        <div class="alerce-spinner"></div>
        <span class="fetching-text">Loading oids...</span>
    </div>

    <!-- Este es el div que HTMX va a reemplazar usando outerHTML y hx-select -->
    <div id="sidebar-inner" class="htmx-indicator-hide">
        {% if objects_list['items']|length > 0 %}
            <input id="objects-store" class="tw-hidden" data-objects="{{ objects_list['items'] }}">
            <input id="selected_oid_cache" class="tw-hidden" value="{{selected_oid}}">

            <div id="sidebar-objects-htmx" class="tw-flex tw-flex-col tw-w-full tw-h-[95vh] tw-text-[0.875rem]">
                
                <div class="tw-px-4 tw-text-black dark:tw-text-white tw-border-y tw-border-[#616161] tw-h-[45px] tw-flex tw-items-center tw-justify-start">
                    Object List
                </div>

                {% for item in objects_list['items']%}
                    {% if item['oid']|string != selected_oid|string %}
                        <div
                        data-oid='{{item["oid"]}}'
                        name="sidebar-row-element" 
                        class="tw-flex tw-items-center tw-justify-start tw-h-[45px] hover:tw-bg-[#b2b2b2]/50 hover:tw-cursor-pointer"
                        hx-get="{{API_URL}}/htmx/side_objects"
                        hx-trigger="click"
                        hx-swap="outerHTML"
                        hx-target="#sidebar-inner"
                        hx-select="#sidebar-inner"
                        hx-indicator="#sidebar-loader"
                        hx-on::config-request="prepare_params(event)"
                        >
                            <span class="tw-px-4 tw-inline-block">{{item['oid']}}</span>
                        </div>
                    {% else %}
                        <div name="sidebar-row-element" class="tw-flex tw-items-center tw-justify-start tw-h-[45px] tw-text-[#1976D2] tw-font-medium tw-bg-[#1976D2]/30">
                            <span class="tw-px-4 tw-inline-block">{{selected_oid}}</span>
                        </div>
                    {% endif %}
                {% endfor %}
            </div>

            <!-- Paginación con estilos reforzados -->
            <div class="tw-flex tw-justify-center tw-items-center tw-text-base tw-h-[48px] tw-text-black dark:tw-text-white">
                {% if has_prev %}
                    <span 
                    id="prev_page"
                    name="side_objects_btn"
                    data-next="{{prev}}"
                    hx-get="{{API_URL}}/htmx/side_objects"
                    hx-trigger="click"
                    hx-swap="outerHTML"
                    hx-target="#sidebar-inner"
                    hx-select="#sidebar-inner"
                    hx-indicator="#sidebar-loader"
                    hx-on::config-request="prepare_params(event)"
                    class="tw-inline-block hover:tw-bg-[#b2b2b2] hover:tw-cursor-pointer"
                    >
                        <svg class="tw-h-[24px] tw-w-[24px] tw-fill-current" viewBox="0 -960 960 960">
                            <path d="M400-240 160-480l240-240 56 58-142 142h486v80H314l142 142-56 58Z"/>
                        </svg>
                    </span>
                {% endif %}
                
                <span id="current_page" data-page="{{objects_list['current_page']}}" class="tw-inline-block tw-text-sm tw-text-center tw-h-[28px] tw-w-[48px] tw-flex tw-items-center tw-justify-center">
                    {{objects_list['current_page']}}
                </span>
                
                {% if has_next %}
                    <span 
                    id="next_page"
                    name="side_objects_btn"
                    data-next="{{next}}"
                    hx-get="{{API_URL}}/htmx/side_objects"
                    hx-trigger="click"
                    hx-swap="outerHTML"
                    hx-target="#sidebar-inner"
                    hx-select="#sidebar-inner"
                    hx-indicator="#sidebar-loader"
                    hx-on::config-request="prepare_params(event)"
                    class="tw-inline-block hover:tw-bg-[#b2b2b2] hover:tw-cursor-pointer"
                    >
                        <svg class="tw-h-[24px] tw-w-[24px] tw-fill-current" viewBox="0 -960 960 960">
                            <path d="m560-240-56-58 142-142H160v-80h486L504-662l56-58 240 240-240 240Z"/>
                        </svg>
                    </span>
                {% endif %}
            </div>
        {% else %}
            <div id="sidebar-objects-htmx" class="tw-flex tw-items-center tw-justify-center tw-h-[100px] tw-text-black/50 dark:tw-text-white">
                No results found
            </div>
        {% endif %}
    </div>
</div>

<script type="module">
    import {init, elementReady} from "{{API_URL}}/static/sidebar.js"
    elementReady("#sidebar-objects-htmx").then(() => {
        init();
    });
</script>
