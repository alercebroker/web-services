name: build_staging_image
on:
  pull_request:
    branches:
      - main

jobs:
  get_tag:
    name: Prepare tags
    runs-on: ubuntu-latest
    outputs:
      tags: ${{ steps.prep.outputs.tags }}
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
      - name: Prepare
        id: prep
        run: |
          DOCKER_IMAGE=ghcr.io/alercebroker/alerts_api
          VERSION=rc
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
          fi
          TAGS="${DOCKER_IMAGE}:${VERSION}"
          if [[ $VERSION =~ ^[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}$ ]]; then
            TAGS="$TAGS,${DOCKER_IMAGE}:rc"
          fi
          echo ::set-output name=tags::${TAGS}

  push_image:
    name: Build image and push to GHCR
    runs-on: ubuntu-latest
    needs: get_tag
    steps:
      - name: Build and push
        uses: alercebroker/ztf_api/.github/workflows/build.yml@develop
        with:
          tag: ${{needs.get_tag.outputs.tags}}
