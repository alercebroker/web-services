name: Tests

on:
  pull_request:
    branches:
      - main

jobs:
  lightcurve_tests:
    uses: ./.github/workflows/test_template.yaml
    with:
        python-version: '3.11'
        sources-folder: 'lightcurve'
        test-folder: 'tests'
        base-folder: 'lightcurve'
