version: "3"
services:
  flask:
    build: .
    image: ztf_api:1.0.0
    env_file:
      - .env
    environment:
      - APP_WORKERS=1
      - APP_THREADS=1
      - prometheus_multiproc_dir=/tmp
      - LOG_LEVEL=info
    ports:
    - 8082:8082
